<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.devst.dao.AdminDAO">

	<select id="selectUserCount" resultType="int">
		SELECT COUNT(mem_id) 
		FROM member_tb;
	</select>

</mapper>